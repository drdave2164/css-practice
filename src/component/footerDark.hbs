{{#with footer}}
<div class="footer_content">
	<div class="footer_links">
		{{#each pages}}
		<a href="{{path}}" class="links">{{name}}</a>
		{{/each}}
	</div>
	<div class="footer_copyrights">{{copyright}}</div>
</div>
{{/with}}